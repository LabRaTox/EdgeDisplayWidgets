"""Backend modules — auto-discovered by the Hub at startup.

Drop a new file in this package, define a `Module` subclass, and decorate
the class with `@register_module`. The Hub picks it up on the next start
without any other wiring.
"""
