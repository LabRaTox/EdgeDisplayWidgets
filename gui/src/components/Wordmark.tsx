/**
 * EDGE//DASH, with the slashes in the accent colour, the same construction as
 * DECK//SWITCH and FACE//CAM, so the family is recognisable at a glance.
 */
export function Wordmark() {
  return (
    <div className="wordmark">
      EDGE<span className="slashes">//</span>DASH
    </div>
  );
}
